InvestigateAI - site update (link-analysis landing + amber map background)
==========================================================================

Drop these into the repo root and commit. Everything else in the repo
(case-files/, evidence/, frameworks/, README.md, favicons, etc.) is unchanged.

NEW files
  index.html    -> the interactive link-analysis MAP (the homepage / site root)
  menu.html     -> the previous text homepage (timeline scroll-spy menu), renamed from index.html
  landing.html  -> identical copy of index.html (optional alias for /landing.html)
  map-bg.js     -> amber animated network drawn in the side gutters of the content pages

CHANGED files (repo content + map background + "<- LINK MAP" header button)
  first-hours.html
  model-examination.html
  regulatory.html

NAVIGATION
  index.html (map)  --[ENTER SITE]-->  menu.html (and section anchors menu.html#scope, etc.)
  every content page  --[<- LINK MAP]-->  index.html (the map)

LATEST TWEAKS in this build
  - landing ribbon: theme switcher removed (brand only); tag reads
    "Open resources for investigating AI incidents" in the mono (Courier) font.
  - no em dashes on the map; the "front door"/"trapped" lines removed.
  - background concentrated/brightened in the side gutters (visible only >1240px wide).
  - mobile map: pointer/touch events (tap a node to inspect), and a small inset
    instead of overscan so edge labels are not cut; uses svh for height.

BEFORE YOU COMMIT
  1. sitemap.xml: add /menu.html; the root now serves the MAP, the text homepage is /menu.html.
  2. Any external links to "/" will now land on the map; the text homepage is /menu.html.
